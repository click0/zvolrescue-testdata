id = "labels-front-pair"
description = "Both front labels (L0, L1) of one member zeroed"
classes = ["labels"]

[[damage]]
member = { leaf = 0 }
range = "label:L0,L1"
pattern = "zeros"

[expect]
# The rear pair still identifies the member.
outcome = "bit-exact"
volumes = "all"
