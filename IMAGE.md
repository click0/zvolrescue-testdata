# The golden image

*Not built yet.* This file is filled in by the build (scenario G1 in
zvolrescue's `docs/REALWORLD-TESTS.md`): environment and OpenZFS version,
`zpool create` layout (members, top-level vdevs, ashift), the datasets and
their properties, the TXG history (what was written between which
snapshots, what was renamed or destroyed at which TXG), the build script's
commit in zvolrescue, and the SHA-256 of every released member file.
